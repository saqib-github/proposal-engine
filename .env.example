# Optional — enables Claude-powered extraction refinement and narrative drafting.
# Without a key the engine runs in deterministic heuristic (NER + RAG) mode.
ANTHROPIC_API_KEY=

# Optional — override the Claude model (default: claude-opus-4-8)
CLAUDE_MODEL=
